{
  "name": "x-coaching",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "preview": "npx serve . -l 8000",
    "dev": "npx live-server --port=8000 --open=./index.html"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "serve": "^14.2.0"
  }
}
